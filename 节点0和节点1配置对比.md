# 节点0和节点1配置对比分析

## 诊断时间
2026年4月17日 上午11:09

## 配置对比

### 节点0配置（有问题）
```
速度限制: 50.0 turns/s  ← 已修改
电流限制: 20.0 A
位置增益: 1.0  ← 已修改
速度增益: 0.02  ← 已修改
速度积分增益: 0.0  ← 已修改
控制模式: 1 (TORQUE_CONTROL)
输入模式: 1 (PASSTHROUGH)
```

### 节点1配置（正常工作）
```
速度限制: 3.0 turns/s  ← 非常低！
电流限制: 10.0 A
位置增益: 20.0  ← 非常高！
速度增益: 0.02
速度积分增益: 0.1
控制模式: 1 (TORQUE_CONTROL)
输入模式: 1 (PASSTHROUGH)
```

## 关键发现 ⚠️⚠️⚠️

### 节点1的速度限制只有 3.0 turns/s，但它能正常工作！

这完全推翻了之前的 OVERSPEED 分析！

**真正的问题不是速度限制太低！**

## 从日志中发现的真相

查看最新日志（11:07-11:08）：

```
[11:07:55] CANDLE TX Set_Axis_State id=0X027 dlc=4 payload=08 00 00 00
           ↑ 节点1的CAN ID (0x027 = 0x020 + 7 = 节点1的Set_Axis_State)

[11:07:58] CANDLE TX Set_Controller_Mode id=0X02B dlc=8 payload=03 00 00 00 01 00 00 00
           ↑ 节点1的CAN ID (0x02B = 0x020 + 11 = 节点1的Set_Controller_Mode)
           控制模式 = 3 (POSITION_CONTROL)

[11:07:58] CANDLE TX Set_Input_Pos id=0X02C dlc=8 payload=00 00 20 41 00 00 00 00
           ↑ 节点1的位置控制命令

[11:08:04] CANDLE TX Set_Controller_Mode id=0X02B dlc=8 payload=01 00 00 00 01 00 00 00
           ↑ 节点1切换到力矩控制

[11:08:04] CANDLE TX Set_Input_Torque id=0X02E dlc=4 payload=00 00 80 3F
           ↑ 节点1的力矩控制命令 (1.0 Nm)

[11:08:10] CANDLE TX Estop id=0X022 dlc=0 payload=<empty>
           ↑ 紧急停止
```

## 真正的问题分析

### 问题1：CAN ID 计算错误

从之前的日志看（10:54-10:58）：
```
[10:54:53] CANDLE TX Set_Controller_Mode id=0X00B dlc=8
           ↑ 0x00B = 节点0的Set_Controller_Mode

[10:57:04] CANDLE TX Set_Controller_Mode id=0X00B dlc=8
[10:57:04] CANDLE TX Set_Input_Vel id=0X00D dlc=8
           ↑ 节点0的速度控制命令
```

**节点0的CAN ID是正确的！**

### 问题2：节点0特定的硬件或配置问题

对比发现：
1. **节点1能用 3.0 turns/s 的低速度限制正常工作**
2. **节点1能用 20.0 的高位置增益正常工作**
3. **节点0即使改成 50.0 turns/s 仍然有问题**

这说明问题不在速度限制或PID参数！

### 问题3：可能是编码器或电机配置问题

让我检查节点0的其他配置：

需要检查：
- 编码器CPR（每转脉冲数）
- 电机极对数
- 电机类型配置
- 编码器类型配置
- 校准状态

## 下一步诊断

需要对比节点0和节点1的完整配置：

```python
import odrive
odrv = odrive.find_any()

print("=== 节点0 ===")
print(f"电机类型: {odrv.axis0.motor.config.motor_type}")
print(f"极对数: {odrv.axis0.motor.config.pole_pairs}")
print(f"编码器CPR: {odrv.axis0.encoder.config.cpr}")
print(f"编码器模式: {odrv.axis0.encoder.config.mode}")
print(f"电机校准: {odrv.axis0.motor.is_calibrated}")
print(f"编码器就绪: {odrv.axis0.encoder.is_ready}")

print("\n=== 节点1 ===")
print(f"电机类型: {odrv.axis1.motor.config.motor_type}")
print(f"极对数: {odrv.axis1.motor.config.pole_pairs}")
print(f"编码器CPR: {odrv.axis1.encoder.config.cpr}")
print(f"编码器模式: {odrv.axis1.encoder.config.mode}")
print(f"电机校准: {odrv.axis1.motor.is_calibrated}")
print(f"编码器就绪: {odrv.axis1.encoder.is_ready}")
```

## 临时结论

**之前的 OVERSPEED 分析是错误的！**

真正的问题可能是：
1. 节点0的编码器配置不正确
2. 节点0的电机参数不匹配
3. 节点0的硬件连接问题
4. 节点0特定的固件bug

需要进一步检查节点0的完整配置才能找到真正原因。
